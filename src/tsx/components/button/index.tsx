import React from 'react';
import './button.css';
import { Button } from 'react-bootstrap';

const WrapButton: React.FC = () => {
  return (
    <Button variant="outline-primary">Primary</Button>
  );
}

export default WrapButton;
